# Bashrc for my work laptop

# Mac ls does not have color...
alias ls="`which ls`";
alias cmr='ssh cominor'

if [ -f /opt/local/etc/bash_completion ]; then
    . /opt/local/etc/bash_completion
fi

# Colors
export CLICOLOR=1
export LSCOLORS=ExFxCxDxBxegedabagacad

# vim: set filetype=sh:
