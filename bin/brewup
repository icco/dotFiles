#! /bin/zsh

brew update
brew upgrade
brew cleanup

cd ~/Projects/dotfiles && \
  brew bundle && \
  git add Brewfile.lock.json && \
  git ci -m 'update brewfile' && \
  git push && \
  cd -

gcloud components update -q

rvm get stable
