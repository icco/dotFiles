# My home server's special stuff for bash

# We use RVM, so don't munge the path.

# F# - Ugh.
export MONO_PATH=~/lib/fsharp/bin
alias fsc="mono $MONO_PATH/fsc.exe --resident"
alias fsi="mono $MONO_PATH/fsi.exe --gui-"

# vim: set filetype=sh:
